<div class="row">
  <div class="three columns leftcolumn">
    <div class="twelve columns artikel">
      <p>
        Binnen de gemeente worden er verschillende activiteiten georganiseerd. 
        Er zijn verschillende samenkomsten, gemeente opbouwende activiteiten en jeugdgroepen.
        Verschillende pastoraat teams dienen onze gemeente.
        Daarnaast zijn er verschillende zendelingen uitgezonden vanuit onze gemeente, 
        worden er evangelisatie acties gedaan.
      </p>
      <p>
        Heeft u vragen over één van deze activiteiten dan kunt u altijd <a href="@Url.Action("Contact", "Index")">contact</a> opnemen.
      </p>
    </div>

    <div class="twelve columns submenucontainer">

      <ul class="submenu">
        @foreach(var part in new string[] { "samenkomsten", "gemeente opbouw", "jeugd", "pastoraat", "evangelisatie", "zending" })
        {
          if(part.Replace(" ", "-") == @ViewBag.PagePart)
          {
          <li class="active">@part</li>
          }
          else
          {
          <li><a href="@Url.Action("Show", "Activities", new { partName = part.Replace(" ", "-") })">@part</a></li>
          }
        }
      </ul>
    </div>
  </div>

  <div class="nine columns artikel">
    <h3>@ViewBag.PageTitle</h3>
    @Html.Raw(ViewBag.PageContent)
    <p>
    </p>
  </div>
</div>
